Determining whether a function is tightly coupled:

Can the unit be tested without mocking dependencies? If not, it's tightly coupled.

The greater the dependencies, the more tightly coupled a function is.

